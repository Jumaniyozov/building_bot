const {
    Product, OrderItem, Order, Locations, ContactData,
    Category, Actions, ProductCategory, Questions,
    User
} = require('../bot/src/models');

module.exports = [Product, OrderItem, Order, Locations, ContactData,
    Category, Actions, ProductCategory, Questions, User];